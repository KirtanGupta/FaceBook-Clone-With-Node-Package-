<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        html, body {
            background-color: black;
            color: grey;
            font-family: monospace;
        }
        #btn {
            float: right;
            margin: 20px;
        }
        #span {
            color: rgb(64, 59, 142);
            font-size: 2.3rem;
        }
        #span2 {
            color: rgb(64, 59, 142);
            font-size: 3.9rem;
            margin: 30px 30px;
        }
        .post {
            border: 1px solid grey;
            height: 150px;
            margin: 10px 30px;
            width: 500px;
        }
        .div {
            height: 70%;
        }
        .div2 button {
            margin: 0 5px;
            background-color: red;
            border: none;
            color: white;
            border-radius: 3px;
            padding: 8px 20px;
        }
        /* Ensure links inside buttons have no default link styling */
        .div2 button a {
            color: white;
            text-decoration: none;
        }
    </style>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <button id="btn" style="background-color: red; padding: 10px 20px;">
        <a href="/logout" style="color: white; text-decoration: none;">Logout</a>
    </button>
    <h1 style="margin: 30px; font-size: 1.5rem;">PROFILE</h1>
    <div class="img" style="overflow: hidden;margin: 10px 30px;height: 100px;border-radius: 12px;width: 100px;">
        <img src="images/uploads/<%= user.profilepic%>" alt="" style="object-fit: cover;height: 100%;width: 100%;">
    </div>
    <h4 style="font-size: 2rem;">
        <span id="span2">HII, <%= user.username %></span>
    </h4>
    <h5 style="font-size: 1.2rem; margin: 10px 30px; ">Create Post Here</h5>
    <form action="/createpost" method="post">
        <textarea name="content" placeholder="Write Your Thoughts ...... " 
                  style="width: 400px; padding: 10px; height: 60px; margin: 10px 30px; background-color: black; color: white;"></textarea>
        <button type="submit" style="margin: 10px 30px; padding: 10px 20px; background-color: rgb(64, 59, 142); border: none; color: white;">
            Create Post
        </button>
    </form>
    <div class="postContainer">
       <% user.post.reverse().forEach((post) => { %> 
            <div class="post">
                <div class="div">
                    <h3 style="margin: 10px 10px;">
                        <span id="span">@<%= user.username %></span>
                    </h3>
                    <p style="margin: 12px 10px;"><%= post.content %></p>
                </div>
                <div class="div2">
                    <button><a href="#">Like</a></button>
                    <button><a href="/edit/<%= post._id %>">Edit</a></button>
                </div>
            </div>
       <% }) %>
    </div>
    <div class=""></div>
</body>
</html>
